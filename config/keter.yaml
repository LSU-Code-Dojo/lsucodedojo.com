exec: ../dist/build/LsuCodeDojo/LsuCodeDojo
args:
    - production
host: lsucodedojo.com
redirects:
    - from: www.lsucodedojo.com
      to: lsucodedojo.com

# Use the following to automatically copy your bundle upon creation via `yesod
# keter`. Uses `scp` internally, so you can set it to a remote destination
copy-to: john@lsucodedojo.com:/opt/keter/incoming
